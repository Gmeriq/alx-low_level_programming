#include "main.h"
#include <string.h>
/**
 * _strcmp - Compare two strings
 * @s1: first parameter
 * @s2: second parameter
 * Return: s1
 */
int _strcmp(char *s1, char *s2)
{
int result;
result = strcmp(s1, s2);
return (result);
}
