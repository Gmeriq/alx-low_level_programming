#include "main.h"
#include <stdio.h>
/**
 *int_isupper- Check case of character
 *Return:1 if uppercase 0 if otherwise
 *@c:variable checked
 */
int_isupper(int c)
{
if (c >= 65 && c <= 90)
return (1);
else
return (0);
}
