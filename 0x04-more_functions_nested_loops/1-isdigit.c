#include "main.h"
/**
 * Description: _isdigit-Check for intergers
 * @c:Variable checked
 * Return:1 when integer 0 when otherwise
 */
int _isdigit(int c)
{
if (c >= 48 && c <= 57)
return (1);
else
return (0);
}
