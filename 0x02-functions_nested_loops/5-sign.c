#include "main.h"
/**
 *Description:print_sign-Find the sign of a number
 *Return: 1 when true 0 when false
 *@n:is the variable
 */
int print_sign(int n)
{
int r;
r = print_sign(98);
if (r < 0)
r *= -1;
_putchar(r + '0');
_putchar('\n');
return (0);
}
