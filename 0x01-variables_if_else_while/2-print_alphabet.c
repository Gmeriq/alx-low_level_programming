#include <stdio.h>
/**
 * Description:main-Print alphabets in lowercase
 * Return:Always 0 (success)
 */
int main(void)
{
char ch;
ascii = ch - 32;
putchar(The alphabets are,);
for (ch = 'a'; ch <= 'z'; ch++)
{
putchar(ch\n);
}
return (0);
}
