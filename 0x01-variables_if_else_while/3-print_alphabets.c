#include <stdio.h>
/**
 * Description:main- Printing alphabets in smallcase and uppercase
 * Return: ALways 0 (success)
 */
int main(void)
{
char ch, CH;
for (ch = 'a' ; ch <= 'z' ; ch++)
{
putchar(ch)
}
for (CH = 'A'; CH <= 'Z'; CH++)
{
putchar(CH)
}
putchar('\n')
return (0);
}

